name: Release Analysis

on: 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  analyze_release:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # إعطاء صلاحية لتعديل الريبو ورفع الـ tags
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # جلب تاريخ Git كامل بما فيه الـ tags

      - name: Run Docker Image
        run: |
          echo "🚀 تشغيل الحاوية..."
          docker run -d --name release-container \
            -p 5000:5000 \
            -e REPO_URL=https://github.com/${{ github.repository }}.git \
            mostfarwag/release:3

          echo "⏳ انتظار بدء تشغيل الحاوية..."
          sleep 5  # الانتظار قليلاً لتجنب أخطاء بدء التشغيل
          
          # التحقق من أن الحاوية بدأت بنجاح
          if [ "$(docker ps -q -f name=release-container)" == "" ]; then
            echo "❌ فشل في تشغيل الحاوية! تحقق من الـ Dockerfile أو الـ Image."
            docker logs release-container || true
            exit 1
          fi

      - name: Wait for API and Script to Complete
        run: |
          echo "📢 متابعة لوجات الحاوية..."
          docker logs -f release-container &
          CONTAINER_PID=$!
          
          # التحقق من أن الحاوية لا تزال تعمل قبل انتظارها
          if [ "$(docker ps -q -f name=release-container)" == "" ]; then
            echo "❌ الحاوية توقفت فجأة! عرض اللوجات ثم الإنهاء."
            docker logs release-container || true
            exit 1
          fi

          echo "⏳ انتظار انتهاء العملية داخل الحاوية..."
          docker wait release-container || true
          
          # قتل عملية متابعة اللوجات لتجنب المشاكل
          kill $CONTAINER_PID || true

      - name: Stop Docker Container
        if: always()
        run: |
          echo "🛑 إيقاف الحاوية..."
          docker stop release-container || true
          docker rm release-container || true
