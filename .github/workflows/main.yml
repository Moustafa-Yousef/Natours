name: Use Release Analysi

on:
  push:
    branches:
      - main

jobs:
  call_reusable_workflow:
    uses: Moustafa-Yousef/ReleaseManagement/.github/workflows/release_analysis.yml@main
    with:
      branch: "main"
    secrets:
      TOKEN: ${{ secrets.GITHUB_TOKEN }}
